{
  "private": true,
  "type": "module",
  "scripts": {
    "cleanup": "php artisan optimize:clear && php artisan config:clear",
    "dev": "start php artisan serve & vite",
    "build": "vite build",
    "build:prod": "php artisan optimize:clear && php artisan config:clear && vite build",
    "clear": "php artisan config:cache && php artisan config:clear && php artisan cache:clear && php artisan optimize",
    "ionic:build": "npm run build",
    "ionic:serve": "npm run dev"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "axios": "^1.6.4",
    "daisyui": "^4.6.0",
    "laravel-vite-plugin": "^1.0.0",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.1",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "apexcharts": "^3.45.2",
    "flowbite": "^2.2.1",
    "spin-wheel": "^4.3.1",
    "unique-username-generator": "^1.3.0"
  },
  "name": "my-app",
  "version": "0.0.0"
}
